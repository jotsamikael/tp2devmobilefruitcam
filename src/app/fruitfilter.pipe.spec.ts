import { FruitfilterPipe } from './fruitfilter.pipe';

describe('FruitfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FruitfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
